# This app has no required environment variables.
#
# The OpenRouter API key is entered by the user in Settings and stored in
# their browser's localStorage. It is forwarded by /api/proxy to
# https://openrouter.ai/api/v1 on each request.
#
# Deploying to Vercel requires no configuration — api/proxy.js runs as a
# serverless function.
